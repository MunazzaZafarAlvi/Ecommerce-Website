<!DOCTYPE html>
<html>
  <head>
    <title>Sign up</title>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <%= csrf_meta_tags %>
  </head>

  <body class="bg-pink-100 h-screen flex items-center justify-center">
    <div class="bg-white p-10 rounded-lg shadow-lg w-96">
      <h2 class="text-3xl font-bold mb-6 text-pink-500 text-center">Sign up</h2>
      
      <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
        <div class="mb-4">
          <%= f.label :email, class: "block text-gray-700" %>
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "w-full p-2 border border-gray-300 rounded mt-1 focus:outline-none focus:ring-2 focus:ring-pink-500" %>
        </div>

        <div class="mb-4">
          <%= f.label :password, class: "block text-gray-700" %>
          <%= f.password_field :password, autocomplete: "new-password", class: "w-full p-2 border border-gray-300 rounded mt-1 focus:outline-none focus:ring-2 focus:ring-pink-500" %>
        </div>

        <div class="mb-4">
          <%= f.label :password_confirmation, class: "block text-gray-700" %>
          <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "w-full p-2 border border-gray-300 rounded mt-1 focus:outline-none focus:ring-2 focus:ring-pink-500" %>
        </div>

        <div class="mb-6">
          <%= f.submit "Sign up", class: "w-full bg-pink-500 text-white p-2 rounded hover:bg-pink-700" %>
        </div>
      <% end %>

      <div class="text-center">
        <%= link_to "Log in", new_session_path(resource_name), class: "text-pink-500 hover:underline" %> |
        <%= link_to "Forgot your password?", new_password_path(resource_name), class: "text-pink-500 hover:underline" %>
      </div>
    </div>
  </body>
</html>
