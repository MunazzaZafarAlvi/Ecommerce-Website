<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Log in</title>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <%= csrf_meta_tags %>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
      body {
        font-family: 'Poppins', sans-serif;
        margin: 0;
        padding: 0;
      }
      .nav-container {
        width: 100%;
        background-color: #333;
        padding: 10px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        color: #fff;
        position: fixed;
        top: 0;
        left: 0;
        z-index: 1000;
      }
      .nav-list {
        list-style: none;
        display: flex;
        gap: 20px;
        margin: 0;
        padding: 0;
      }
      .profile {
        display: flex;
        gap: 15px;
        align-items: center;
      }
    </style>
  </head>

  <body class="bg-gradient-to-r from-pink-200 to-purple-300 h-screen flex items-center justify-center">
    <div class="nav-container">
      <div style="font-size: 24px; font-family: 'Poppins', sans-serif;">DESIRES</div>
      <ul class="nav-list">
        <li><%= link_to 'Home', root_path, style: "color: #fff;" %></li>
        <li><%= link_to 'About Us', about_us_path, style: "color: #fff;" %></li>
        <li><%= link_to 'Products', products_path, style: "color: #fff;" %></li>
        <li><%= link_to 'FAQ', faq_path, style: "color: #fff;" %></li>
      </ul>
      <div class="profile">
        <% if customer_signed_in? %>
          <%= image_tag current_customer.profile_picture_url, alt: 'Profile', size: "30x30", style: "border-radius: 50%;" if current_customer.profile_picture_url.present? %>
          <%= current_customer.username %>
          <%= link_to 'Logout', destroy_customer_session_path, method: :delete, style: "color: #fff;" %>
        <% end %>
      </div>
    </div>

    <div class="bg-white p-10 rounded-lg shadow-lg w-96 mt-20">
      <h2 class="text-4xl font-extrabold mb-6 text-pink-600 text-center">Log in</h2>
      
      <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <div class="mb-4">
          <%= f.label :email, class: "block text-gray-700" %>
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "w-full p-2 border border-gray-300 rounded mt-1 focus:outline-none focus:ring-2 focus:ring-pink-500" %>
        </div>

        <div class="mb-4">
          <%= f.label :password, class: "block text-gray-700" %>
          <%= f.password_field :password, autocomplete: "current-password", class: "w-full p-2 border border-gray-300 rounded mt-1 focus:outline-none focus:ring-2 focus:ring-pink-500" %>
        </div>

        <div class="mb-4 flex items-center">
          <%= f.check_box :remember_me, class: "mr-2" %>
          <%= f.label :remember_me, class: "text-gray-700" %>
        </div>

        <div class="mb-6">
          <%= f.submit "Log in", class: "w-full bg-pink-600 text-white p-2 rounded hover:bg-pink-700 transition duration-200" %>
        </div>
      <% end %>

      <div class="text-center">
        <%= link_to "Sign up", new_registration_path(resource_name), class: "text-pink-500 hover:underline transition duration-200" %> |
        <%= link_to "Forgot your password?", new_password_path(resource_name), class: "text-pink-500 hover:underline transition duration-200" %>
      </div>
    </div>
  </body>
</html>

